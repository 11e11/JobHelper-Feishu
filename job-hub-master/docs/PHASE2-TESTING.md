# 阶段二测试说明

阶段二状态机只监听用户真实提交。明确成功后，缺失信息进入补全窗口；明确失败不写飞书；冲突或超时由用户确认。

飞书写入采用公司和岗位编号、公司和岗位名称及地点、规范化官网 URL、投递标识的顺序查重。更新只发送白名单非空字段，不修改流程记录、结束状态、后续事项、匹配度、备注和进行中标记。

自动测试运行：`npm test`。真实验收时由用户登录、上传简历并点击最终提交，扩展只检查结果和同步记录。
