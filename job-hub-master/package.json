{
  "name": "job-hub-personal-safe",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "node --check content/fill-engine.js && node --check content/result-monitor.js && node --check content/scraper.js && node --check sidepanel/resume-fill.js && node --check sidepanel/job-tracker.js && node --check options/options.js && node --check service-worker.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
