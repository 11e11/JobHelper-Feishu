const fields=[['name','姓名'],['phone','手机号'],['email','邮箱'],['currentCity','所在城市'],['school','学校'],['college','学院'],['major','专业'],['education','学历'],['enrollmentDate','入学时间'],['graduationDate','毕业时间'],['gender','性别'],['birthDate','出生日期'],['politicalStatus','政治面貌'],['ethnicity','民族'],['github','GitHub'],['homepage','个人主页'],['portfolio','作品集'],['jobDirection','求职方向'],['source','信息来源'],['expectedCity','期望城市（每次确认）'],['acceptTransfer','是否接受调剂（每次确认）'],['acceptOtherRole','是否接受其他岗位（每次确认）'],['expectedSalary','期望薪资（每次确认）'],['relativeEmployed','是否有亲属任职（每次确认）'],['nonCompete','是否签署竞业协议（每次确认）']] as const;
const form=document.querySelector<HTMLFormElement>('#profile')!;form.innerHTML=fields.map(([k,l])=>`<label>${l}<input name="${k}" autocomplete="off"></label>`).join(''); void chrome.storage.local.get('profile').then(({profile={}})=>fields.forEach(([k])=>(form.elements.namedItem(k) as HTMLInputElement).value=profile[k]??''));form.onsubmit=async e=>{e.preventDefault();const profile=Object.fromEntries(new FormData(form).entries());await chrome.storage.local.set({profile});document.querySelector('#status')!.textContent='已保存到本机浏览器';};
