import {beforeEach,describe,expect,it,vi} from 'vitest';import {fillSafe,undoLastFill} from '../src/core/filler';import {DictionaryDetector} from '../src/core/detector';
describe('safe filler',()=>{beforeEach(()=>document.body.innerHTML='');it('触发 input/change/blur 并可撤销',()=>{document.body.innerHTML='<label>姓名<input value="旧值"></label>';const el=document.querySelector('input')!;const input=vi.fn(),change=vi.fn(),blur=vi.fn();el.addEventListener('input',input);el.addEventListener('change',change);el.addEventListener('blur',blur);const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{name:'张三'})).toBe(1);expect(el.value).toBe('张三');expect(input).toHaveBeenCalled();expect(change).toHaveBeenCalled();expect(blur).toHaveBeenCalled();expect(undoLastFill()).toBe(1);expect(el.value).toBe('旧值');});it('不填写确认、禁止和低置信度字段，也不提交',()=>{document.body.innerHTML='<label>期望薪资<input></label><label>身份证号码<input></label><button type="submit">提交</button>';const submit=vi.fn();document.addEventListener('submit',submit);const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{expectedSalary:'20k'})).toBe(0);expect(submit).not.toHaveBeenCalled();});});
describe('safe filler',()=>{beforeEach(()=>document.body.innerHTML='');it('触发 input/change/blur 并可撤销',()=>{document.body.innerHTML='<label>姓名<input value="旧值"></label>';const el=document.querySelector('input')!;const input=vi.fn(),change=vi.fn(),blur=vi.fn();el.addEventListener('input',input);el.addEventListener('change',change);el.addEventListener('blur',blur);const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{name:'张三'})).toBe(1);expect(el.value).toBe('张三');expect(input).toHaveBeenCalled();expect(change).toHaveBeenCalled();expect(blur).toHaveBeenCalled();expect(undoLastFill()).toBe(1);expect(el.value).toBe('旧值');});it('批量不填写确认、禁止和低置信度字段，也不提交',()=>{document.body.innerHTML='<label>期望薪资<input></label><label>身份证号码<input></label><button type="submit">提交</button>';const submit=vi.fn();document.addEventListener('submit',submit);const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{expectedSalary:'20k'})).toBe(0);expect(submit).not.toHaveBeenCalled();});it('只在显式确认后单项填写确认类字段',()=>{document.body.innerHTML='<label>期望薪资<input></label>';const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{expectedSalary:'20k'},m[0].elementId)).toBe(0);expect(fillSafe(m,{expectedSalary:'20k'},m[0].elementId,true)).toBe(1);expect(document.querySelector('input')!.value).toBe('20k');});});
describe('complex controls',()=>{beforeEach(()=>document.body.innerHTML='');it('填写原生下拉与单选',()=>{document.body.innerHTML='<label>最高学历<select><option>本科</option><option>硕士</option></select></label><fieldset>性别<label><input type="radio" name="g" value="男">男</label><label><input type="radio" name="g" value="女">女</label></fieldset>';const m=new DictionaryDetector().scan(document);expect(fillSafe(m,{education:'硕士',gender:'女'})).toBeGreaterThanOrEqual(2);expect(document.querySelector('select')!.value).toBe('硕士');expect((document.querySelector('input[value="女"]') as HTMLInputElement).checked).toBe(true);});it('使用原生 setter 支持受控输入并触发事件',()=>{document.body.innerHTML='<label>所在城市<input></label>';const el=document.querySelector('input')!;const input=vi.fn();el.addEventListener('input',input);expect(fillSafe(new DictionaryDetector().scan(document),{currentCity:'长沙'})).toBe(1);expect(el.value).toBe('长沙');expect(input).toHaveBeenCalledOnce();});});
